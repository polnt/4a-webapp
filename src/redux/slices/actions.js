import { openModal, closeModal, logout } from "./modalSlice";
import { setAuth, signIn, signOut } from "./authSlice";
import { setGlobalAlert } from "./globalAlertSlice";

export {
  openModal,
  closeModal,
  logout,
  setAuth,
  signIn,
  signOut,
  setGlobalAlert,
};
